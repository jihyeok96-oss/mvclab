spring:
  application:
    name: spring-mvc-lab
  datasource:
    url: jdbc:postgresql://localhost:5432/company
    username: postgres
    password: 1004
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: always

  thymeleaf:
    cache: false
